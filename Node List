#include <stdio.h>
#include <stdlib.h>

#define true 1
#define false 0

typedef struct NODE
{
    int value;
    struct NODE * next;
} NODE;

void addNode(int value, NODE* root)

int main(void)
{
    NODE rootNode;
    rootNode.value = 5;
    rootNode.next = NULL;
    NODE secondNode;
    secondNode.value = 8;
    secondNode.next = NULL;
    rootNode.next = &secondNode;
    printf("%i\n", secondNode.value);
     printf("%i\n", rootNode.value);
          printf("%i\n", rootNode.next->value);
}
