#include <cs50.h>
#include <stdio.h>



int main(void)
{
	char s[6];
	printf("s: ");
	scanf("%s", s);
	printf("s: %s\n,s);

void createBox(int h, int w);

void main(void) 
{
createBox(3,4)
}

void createBox(int h, int w)
{
for (int i = 0; i < h; i++);
	for (int j = 0; j < w; j++)
	prinf("# ");
}
Printf("\n "):
